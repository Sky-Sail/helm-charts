{{- if and .Values.gateway.enabled .Values.gateway.nodeManagement.enabled (or .Values.gateway.nodeManagement.gatewayName .Values.gateway.gatewayName .Values.gateway.nodeManagement.parentRefs) }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "pritunl-zero.fullname" . }}-node-management
  labels:
    {{- include "pritunl-zero.labels" . | nindent 4 }}
    app.kubernetes.io/component: node-management
  {{- with .Values.gateway.nodeManagement.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.gateway.nodeManagement.parentRefs }}
  parentRefs:
    {{- range .Values.gateway.nodeManagement.parentRefs }}
    - name: {{ .name }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- else }}
      namespace: {{ $.Release.Namespace }}
      {{- end }}
      {{- if .sectionName }}
      sectionName: {{ .sectionName }}
      {{- end }}
    {{- end }}
  {{- else if .Values.gateway.nodeManagement.gatewayName }}
  parentRefs:
    - name: {{ .Values.gateway.nodeManagement.gatewayName }}
      {{- if .Values.gateway.nodeManagement.gatewayNamespace }}
      namespace: {{ .Values.gateway.nodeManagement.gatewayNamespace }}
      {{- else if .Values.gateway.gatewayNamespace }}
      namespace: {{ .Values.gateway.gatewayNamespace }}
      {{- else }}
      namespace: {{ .Release.Namespace }}
      {{- end }}
  {{- else if .Values.gateway.gatewayName }}
  parentRefs:
    - name: {{ .Values.gateway.gatewayName }}
      {{- if .Values.gateway.gatewayNamespace }}
      namespace: {{ .Values.gateway.gatewayNamespace }}
      {{- else }}
      namespace: {{ .Release.Namespace }}
      {{- end }}
  {{- end }}
  hostnames:
    {{- if .Values.gateway.nodeManagement.domains }}
    {{- range .Values.gateway.nodeManagement.domains }}
    - {{ . | quote }}
    {{- end }}
    {{- else }}
    {{- range .Values.gateway.nodeManagement.hosts }}
    - {{ .hostname | quote }}
    {{- end }}
    {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      {{- if .Values.gateway.nodeManagement.filters }}
      filters:
        {{- toYaml .Values.gateway.nodeManagement.filters | nindent 8 }}
      {{- end }}
      backendRefs:
        - name: {{ include "pritunl-zero.fullname" . }}
          port: {{ .Values.service.port }}
{{- end }}

