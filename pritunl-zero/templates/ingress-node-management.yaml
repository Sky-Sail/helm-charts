{{- if and .Values.ingress.enabled .Values.ingress.nodeManagement.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "pritunl-zero.fullname" . }}-node-management
  labels:
    {{- include "pritunl-zero.labels" . | nindent 4 }}
    app.kubernetes.io/component: node-management
  {{- with .Values.ingress.nodeManagement.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.nodeManagement.className }}
  ingressClassName: {{ . }}
  {{- else with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.nodeManagement.tls }}
  tls:
    {{- range .Values.ingress.nodeManagement.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.ingress.nodeManagement.domains }}
    {{- range .Values.ingress.nodeManagement.domains }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "pritunl-zero.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
    {{- end }}
    {{- else }}
    {{- range .Values.ingress.nodeManagement.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "pritunl-zero.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
    {{- end }}
    {{- end }}
{{- end }}

